# Copy this file to .env.local and fill in your InsForge project URL.
# This is the ONLY env var the frontend needs — it points the typed client at
# your edge functions. It is public (NEXT_PUBLIC_) by design; no secrets here.
NEXT_PUBLIC_INSFORGE_URL=https://YOUR-PROJECT.insforge.dev
