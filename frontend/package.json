{
  "name": "debate-the-wizard-pixel",
  "version": "1.0.0",
  "private": true,
  "description": "Debate the Wizard - pixel Game Boy battle frontend (static, no build)",
  "scripts": {
    "generate-env": "node scripts/generate-env.js",
    "dev": "npm run generate-env && npx -y http-server . -p 3000 -c-1",
    "build": "npm run generate-env",
    "vercel-build": "npm run generate-env"
  },
  "devDependencies": {
    "dotenv": "^17.4.2"
  }
}
