// Generated by npm run build:functions. Deploy with: npx @insforge/cli functions deploy health --file backend/functions/dist/health.ts

// backend/functions/src/shared/env.ts
function env(key, fallback = "") {
  return Deno.env.get(key) ?? fallback;
}

// backend/functions/src/shared/http.ts
var corsHeaders = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "Content-Type, Authorization",
  "Content-Type": "application/json"
};
function withMethods(methods) {
  return {
    ...corsHeaders,
    "Access-Control-Allow-Methods": [...methods, "OPTIONS"].join(", ")
  };
}
function options(methods = ["GET", "POST"]) {
  return new Response("ok", { headers: withMethods(methods) });
}

// backend/functions/src/entrypoints/health.ts
function health_default(req) {
  if (req.method === "OPTIONS") return options(["GET"]);
  const body = {
    ok: true,
    service: "debate-the-wizard",
    time: (/* @__PURE__ */ new Date()).toISOString(),
    config: {
      gateway: Boolean(env("OPENROUTER_API_KEY") || env("INSFORGE_API_URL")),
      db: Boolean(env("INSFORGE_API_KEY")),
      youcom: Boolean(env("YOUCOM_API_KEY"))
    }
  };
  return new Response(JSON.stringify(body), { status: 200, headers: withMethods(["GET"]) });
}
export {
  health_default as default
};
